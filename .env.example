# Cetus Rebalance Bot Configuration

# Required: Your wallet private key (64 hex characters, with or without 0x prefix)
# WARNING: Never share or commit your private key!
PRIVATE_KEY=your_private_key_here

# Required: Network to connect to (mainnet or testnet)
NETWORK=mainnet

# RPC Configuration
# Option 1: Single RPC URL
RPC_URL=https://fullnode.mainnet.sui.io

# Option 2: Multiple RPC URLs for failover (comma-separated)
# RPC_URLS=https://rpc1.mainnet.sui.io,https://rpc2.mainnet.sui.io,https://rpc3.mainnet.sui.io

# Bot Configuration
# How often to check positions (in seconds)
CHECK_INTERVAL_SECONDS=30

# Slippage tolerance for transactions (in percent, e.g., 0.5 = 0.5%)
SLIPPAGE_PERCENT=0.5

# Gas budget for transactions (in MIST, 1 SUI = 1_000_000_000 MIST)
# Default: 100000000 (0.1 SUI)
GAS_BUDGET=100000000

# Enable/disable actual rebalancing (set to false for monitoring only)
REBALANCE_ENABLED=true

# Logging level (error, warn, info, debug)
LOG_LEVEL=info
